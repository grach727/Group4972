// Write a programm which sorts the elements of each row from max to min.

// 1) Sorting

void SortArr(int[,] arr)
{
    int buf = new.int[arr.GettLength(1)];
    for (int = 0; i < arr.GetLength(0); int++)
    {
        for (int j = 0; int < arr.GetLength(1); j++)
        {
            buf[i] = arr[i,j];
        }
    }
}